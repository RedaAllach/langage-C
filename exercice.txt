#include <stdio.h>
#include <stdlib.h>


/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	char name[50];
	char prenom[50];
	int age;
	int tele;
	char sexe[50];
	printf("donner votre nom");
	scanf("%s",&name);
	printf("donner votre prenom");
	scanf("%s",&prenom);
	printf("donner votre age");
	scanf("%d",&age);
	printf("donner votre tele");
	scanf("%d",&tele);
	printf("donner votre sexe");
	scanf("%s",&sexe);
	printf("bonjour %s %s et votre age est %d et votre tele est %d et votre sexe est %s ",name,prenom,age,tele,sexe);
	
	return 0;
}
----------------------------------------------------------------------------------------------------------------
#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	float f,c;
	printf("la  temperature de Fahrenheit");
	scanf("%f",&f);
	
	//f= (C*1.8)+32
	c=(f-32)/1.8;
	printf("la temperature en celusis est %f",c);
	
	return 0;

}
---------------------------------------------------------------------------------------------------------------
#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
//challenge MILe
int main() {
	float d,mille;
	
	printf("la distance en metre");
	scanf("%f",&d);
	mille=d*1000*1.609;
	printf("la transforme en Mile est %f",mille);
	
	return 0;
}
------------------------------------------------------------------------------------------------------------------------------
#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	float d,mettre;
	printf("la distance en mettre");
	scanf("%f",&d);
	mettre=d+1000/1.609;
	printf("la transforme en mettre est %f,mettre");
	


	
	return 0;
}
--------------------------------------------------------------------------------------------------------------------------------------
#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a , b , c, d;
	int somme;
    float moyenne;
        printf("donner moi les quatres valeurs \n");
        scanf("%d",&a);
        scanf("%d",&b);
        scanf("%d",&c);
        scanf("%d",&d);
	somme= a + b + c + d;
	printf("la somme est %d",somme);
	moyenne = somme/4;
	printf("moyenne est %f",moyenne);
	return 0;
}